# Brewfile — declared CI tool dependencies.
# Homebrew cannot hard-pin arbitrary old formula versions, so these are
# recorded here for visibility/intent. Update the version comments when
# you intentionally upgrade.
#
# Expected versions (as of 2026-06):
#   swiftformat ~0.61.x  (0.61.1 tested)
#   xcodegen    ~2.44.x  (2.44.1 tested)

brew "swiftformat"
brew "xcodegen"
