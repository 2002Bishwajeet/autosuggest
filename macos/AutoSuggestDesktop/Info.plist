<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleDisplayName</key>
    <string>AutoSuggest</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSApplicationCategoryType</key>
    <string>public.app-category.productivity</string>
    <key>LSUIElement</key>
    <true/>
    <key>NSHighResolutionCapable</key>
    <true/>
    <key>NSPrincipalClass</key>
    <string>NSApplication</string>
    <key>NSAccessibilityUsageDescription</key>
    <string>AutoSuggest needs Accessibility permission to read text context and insert completions in any application.</string>
    <key>NSAppleEventsUsageDescription</key>
    <string>AutoSuggest uses Apple Events as a fallback for text insertion when direct Accessibility insertion is unavailable.</string>
    <!-- Sparkle auto-update configuration. See docs/SPARKLE_SETUP.md for the
         maintainer activation checklist. -->
    <!-- Sparkle appcast feed, served straight from the GitHub Release assets.
         The release workflow attaches a signed appcast.xml to each release, and
         `releases/latest/download/…` always redirects to the newest release's
         asset — so this URL needs no extra hosting. Binaries download from the
         same release. -->
    <key>SUFeedURL</key>
    <string>https://github.com/2002Bishwajeet/autosuggest/releases/latest/download/appcast.xml</string>
    <!-- EdDSA (ed25519) PUBLIC key from Sparkle's generate_keys. Sparkle verifies
         every downloaded update against it; the matching PRIVATE key lives only in
         the maintainer's Keychain + the SPARKLE_EDDSA_PRIVATE_KEY CI secret. -->
    <key>SUPublicEDKey</key>
    <string>RSkV/8EETWJhFj4aBBFRWupsbnJfKIWxiKynh0ieDwk=</string>
    <key>SUEnableAutomaticChecks</key>
    <true/>
    <!-- Check for updates once per day (in seconds). -->
    <key>SUScheduledCheckInterval</key>
    <integer>86400</integer>
</dict>
</plist>
